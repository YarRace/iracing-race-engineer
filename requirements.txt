# Всё, без чего программа не работает целиком. Проверяется тестом
# tests/test_requirements.py: он читает импорты из кода и сверяет с этим
# списком. Забытая строчка здесь стоила трёх кругов красного прогона —
# Pillow стоял на машине Ярослава и не стоял на раннере, а падало это в
# сборке картинок, за три шага от настоящей причины.

pyirsdk                 # SDK симулятора
fastapi
uvicorn[standard]
httpx
anthropic               # разбор заезда
PySide6                 # оверлей и панель настроек

# Голосовой перевод командного радио
soundcard
deep-translator
faster-whisper
numpy                   # translator.py импортирует напрямую, не через whisper

pygame                  # кнопки руля и хаты: без него привязки молча не работают
pillow                  # иконка и картинка для мессенджеров (tools/)

pytest
